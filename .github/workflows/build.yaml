on:
  push:
    branches:
      - '*'
jobs:
  build-all-examples:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: 11
      - run: pushd connect-pattern && ./gradlew test && popd
      - run: pushd custom-adapter-and-fake && ./gradlew test && popd
      - run: pushd using-connect-adapters && ./gradlew test && popd
